/** Minimal portfolio route: one background, text, and accent system. */
import PortfolioExperience from "./PortfolioExperience";

export default function Home() {
  return <PortfolioExperience variant="minimal" />;
}
